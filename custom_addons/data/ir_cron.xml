<odoo>
    <data noupdate="1">
        <record id="ir_cron_passport_deadline_reminder" model="ir.cron">
            <field name="name">Passport Deadline Reminder</field>
            <field name="model_id" ref="product.model_product_template" />
            <field name="state">code</field>
            <field name="code">model._check_passport_deadline()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall"
                eval="(DateTime.now().replace(hour=5, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="active">True</field>
            <field name="groups_id" eval="[(4, ref('custom_addons.group_custom_azerbaijan_user'))]" />
        </record>

        <record id="ir_cron_worksheet_task_automation" model="ir.cron">
            <field name="name">Worksheet Task Automation</field>
            <field name="model_id" ref="project.model_project_project" />
            <field name="state">code</field>
            <field name="code">model._cron_create_tasks()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall"
                eval="(DateTime.now().replace(hour=1, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="active">False</field>
            <field name="groups_id" eval="[(4, ref('custom_addons.group_custom_georgia_user'))]" />
        </record>
    </data>
</odoo>